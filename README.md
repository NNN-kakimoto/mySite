# virtual machine setting
on Gitbash
```
	$ cd ~/vagrant/
	$ vagrant up
	$ vagrant ssh
	$ cd /vagrant/mySite
	$ jekyll serve --host 0.0.0.0 --baseurl '' -w
 ```

# Git
``` 
	$ git commit 
	$ git push origin master
 ```
 
